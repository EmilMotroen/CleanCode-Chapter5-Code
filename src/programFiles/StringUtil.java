package programFiles;

public class StringUtil {
    public static boolean isBlank(String pageName) {
        if (pageName.isBlank()){
            return true;
        }
        return false;
    }
}
